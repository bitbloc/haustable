# แผนการส่งต่อและอัพเดทระบบ Yuzu AI ไปยังเซิร์ฟเวอร์หลัก (Migration & Handover Plan)

เอกสารฉบับนี้จัดทำขึ้นเพื่อให้คุณสามารถคัดลอกไฟล์และนำไปอัพเดท/Deploy บนเซิร์ฟเวอร์หลักที่ใช้รัน LINE Bot (Yuzu AI) ได้อย่างถูกต้องและปลอดภัย

---

## 📋 ภาพรวมของส่วนประกอบที่ต้องอัพเดท
มีทั้งหมด 3 ส่วนหลักที่ได้รับการพัฒนาและปรับปรุงประสิทธิภาพ:
1. **`supabase/functions/stock-api`**: ตัวจัดการ API สำหรับระบบ Stock วัตถุดิบ ที่เพิ่มความปลอดภัยในการบันทึกประเภทหน่วย (Unit Mismatch Check) และป้องกันปัญหากับดักตัวแปลงหน่วยเริ่มต้นเป็น 1
2. **`supabase/functions/line-webhook`**: เว็บฮุกของ LINE Bot (Yuzu AI) ที่ถอดระบบค้นหา Makro Pro (เนื่องจาก API เดิมปิดตัวลง) แล้วเปลี่ยนไปเป็นระบบ **General Web Search (DuckDuckGo Scraper)** เพื่อดึงข้อมูลราคากลางแทน โดยรองรับทั้งการพิมพ์ `/ราคา [ชื่อสินค้า]` และ `makro [ชื่อสินค้า]`
3. **`supabase/functions/search-api`**: ฟังก์ชันค้นหาราคากลางสำหรับเรียกใช้งานจากฝั่งหน้าบ้าน (Frontend Modal)

---

## 🛠️ ขั้นตอนที่ 1: การอัพเดทโค้ดบน Repository ของเซิร์ฟเวอร์หลัก

ก่อนทำการ Deploy ให้คัดลอกเนื้อหาของไฟล์ Edge Functions จากโปรเจ็กต์ปัจจุบันไปทับใน Repository ของเซิร์ฟเวอร์หลัก ดังนี้:

### 1. ไฟล์ระบบความปลอดภัยและ API สต็อก (`stock-api`)
* คัดลอกโค้ดทั้งหมดจาก: `supabase/functions/stock-api/index.ts`
* ไปทับที่เซิร์ฟเวอร์หลัก: `supabase/functions/stock-api/index.ts`
* *จุดเด่นที่เพิ่มเข้ามา:* ระบบจะปฏิเสธการเซฟสินค้าวัตถุดิบผ่าน API ทันที หากพบว่าหน่วยซื้อ (`pack_unit`) และหน่วยใช้งาน (`usage_unit`) เป็นคนละประเภทกัน (เช่น กิโลกรัม กับ มิลลิลิตร) แต่ไม่มีการระบุตัวแปลงหน่วย (`conversion_factor`) ที่เหมาะสมเข้ามา

### 2. ไฟล์บอทเว็บฮุก (`line-webhook`)
* คัดลอกโค้ดทั้งหมดจาก: `supabase/functions/line-webhook/index.ts`
* ไปทับที่เซิร์ฟเวอร์หลัก: `supabase/functions/line-webhook/index.ts`
* *จุดเด่นที่เพิ่มเข้ามา:* อัพเกรดคำสั่งเช็คราคาเป็นตัวใหม่ และจำกัดปริมาณข้อมูลผลลัพธ์ไม่เกิน 5 หน้า Carousel ในกรณีสินค้าหมดคลัง/ต้องซื้อจำนวนมาก เพื่อป้องกันไม่ให้ LINE API ปฏิเสธส่งข้อความเนื่องจากข้อมูลเกินขนาด 50KB

### 3. ไฟล์ค้นหาราคากลางหน้าเว็บ (`search-api` - ถ้ายังไม่มีในระบบหลัก)
* คัดลอกโค้ดทั้งหมดจาก: `supabase/functions/search-api/index.ts`
* ไปวางที่เซิร์ฟเวอร์หลัก: `supabase/functions/search-api/index.ts`
* สร้างไฟล์ตั้งค่า Deno (ถ้าไม่มี): `supabase/functions/deno.json`

---

## 🔑 ขั้นตอนที่ 2: การตรวจสอบค่าคอนฟิกบนเซิร์ฟเวอร์หลัก

### 1. ตั้งค่าความปลอดภัยสำหรับ Edge Functions
ตรวจสอบว่าโปรเจ็กต์ Supabase หลักของคุณได้ตั้งค่าตัวแปรสภาพแวดล้อม (Environment Secrets) ไว้เรียบร้อยแล้ว โดยรันคำสั่งด้านล่างนี้ใน Terminal ของโปรเจ็กต์หลัก (แทนที่ค่าคีย์ด้วยคีย์จริงของคุณ):

```bash
# ตั้งค่า API Key สำหรับความปลอดภัยภายในระบบ Stock API
supabase secrets set STOCK_API_KEY="ใส่คีย์ความปลอดภัยระบบสต็อกของคุณที่นี่"
```

หรือสามารถตรวจสอบผ่านเมนูใน Supabase Dashboard:
**Settings > API > Edge Function Secrets**

### 2. ตรวจสอบข้อมูลในตาราง `app_settings`
ระบบ LINE Webhook จะดึงคีย์การเชื่อมต่อโดยตรงจากตาราง `app_settings` ในฐานข้อมูล โปรดตรวจสอบให้แน่ใจว่าโปรเจ็กต์หลักของคุณมีคีย์ 2 ตัวนี้อยู่:
* `line_channel_secret` (เก็บค่า Channel Secret ของบอท)
* `line_channel_access_token` (เก็บค่า Channel Access Token ของบอท)

*(หากไม่มี สามารถรัน SQL Script นี้ใน SQL Editor ของ Supabase หลักเพื่อเพิ่มได้):*
```sql
INSERT INTO app_settings (key, value) VALUES 
('line_channel_secret', 'ค่า_channel_secret_ของคุณ'),
('line_channel_access_token', 'ค่า_access_token_ของคุณ')
ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
```

---

## 🚀 ขั้นตอนที่ 3: การสั่ง Deploy ไปยังเซิร์ฟเวอร์หลัก

รันคำสั่งเหล่านี้จาก Terminal ของโปรเจ็กต์ที่มี Supabase CLI เพื่ออัพโหลดฟังก์ชันขึ้นไปยังโปรเจ็กต์หลัก:

> [!IMPORTANT]
> แทนที่ `[PROJECT_REF_หลัก]` ด้วย **Project ID/Reference ID** ของ Supabase หลักตัวที่คุณใช้งานจริง (เช่น `lxfavbzmebqqsffgyyph` หรือตัวอื่นของเซิร์ฟเวอร์หลัก)

```bash
# 1. Deploy ระบบ Stock API (ปิดการตรวจสอบ JWT เนื่องจากเราตรวจสอบด้วย x-internal-api-key ในโค้ดแล้ว)
npx supabase functions deploy stock-api --project-ref [PROJECT_REF_หลัก] --no-verify-jwt

# 2. Deploy ค้นหาราคากลางหน้าเว็บ
npx supabase functions deploy search-api --project-ref [PROJECT_REF_หลัก] --no-verify-jwt

# 3. Deploy เว็บฮุกของ LINE Bot
npx supabase functions deploy line-webhook --project-ref [PROJECT_REF_หลัก] --no-verify-jwt
```

---

## 🤖 คู่มือคำสั่งบอท Yuzu AI หลังอัพเดท (LINE Bot Commands Manual)

เมื่อคุณเชื่อมต่อ Webhook URL จาก LINE Developers Console ไปยังหน้าลิ้งค์ใหม่ของเว็บฮุกเซิร์ฟเวอร์หลักแล้ว สมาชิกในกลุ่มไลน์สามารถใช้งานคำสั่งเหล่านี้ได้ทันที:

### 1. คำสั่งทดสอบระบบ
* **`ping`**
  * *การทำงาน:* บอทจะตอบกลับว่า `Pong! 🏓 (Webhook is working)` เพื่อคอนเฟิร์มว่าระบบหลังบ้านเชื่อมต่อกับ LINE ได้ถูกต้อง

### 2. คำสั่งค้นหาราคากลาง (ใหม่ แทนที่ระบบ Makro)
* **`ราคา [ชื่อวัตถุดิบ]`** หรือ **`makro [ชื่อวัตถุดิบ]`** (ใช้ได้ทั้งสองคำสั่ง)
  * *การทำงาน:* ค้นหาข้อมูลสินค้าและราคาขายล่าสุดจาก Google/DuckDuckGo ทันที
  * *ผลลัพธ์:* แสดงการ์ด Flex Message สวยงาม 5 อันดับแรก พร้อมไฮไลท์ราคาที่พบ (เช่น ฿45, 120 บาท) แหล่งที่มา (เช่น makroclick.com, bigc.co.th) และรายละเอียดสินค้า เพื่อเปรียบเทียบหาต้นทุนที่แม่นยำที่สุด

### 3. คำสั่งจัดการสต็อกวัตถุดิบ
* **`stbuy`**
  * *การทำงาน:* แสดงรายการสินค้าวัตถุดิบทั้งหมดที่มีจำนวนเหลือต่ำกว่าหรือเท่ากับจุดแจ้งเตือนขั้นต่ำ (`min_stock_threshold` หรือ `reorder_point`) หรือของหมดสต็อก
  * *ผลลัพธ์:* จัดส่งรูปแบบ Flex Carousel สีสันพรีเมียม (จำกัดความยาวสูงสุดไม่เกิน 5 หน้า บล็อกละไม่เกิน 19 รายการ เพื่อหลีกเลี่ยงข้อจำกัดของไลน์) แบ่งประเภทระดับความวิกฤต เช่น:
    * ⚫ **หมด** (สต็อกเป็น 0)
    * 🔴 **วิกฤต** (ต่ำกว่าระดับแจ้งเตือนขั้นต่ำ)
    * 🟠 **ต้องเติม** (ต่ำกว่าจุดสั่งซื้อ)
* **`stbuyback`**
  * *การทำงาน:* แสดงเฉพาะรายการสินค้าต้องซื้อที่เพิ่งมีการอัพเดทความเคลื่อนไหวภายใน 24 ชั่วโมงที่ผ่านมา

### 4. คำสั่งประวัติสต็อกและการปรับปรุงสต็อก
* **`stday`** - สรุปรายการทำรายการถอน/เพิ่ม/ปรับปรุงสต็อกของ**วันนี้**ทั้งหมด
* **`stback`** - สรุปประวัติธุรกรรมสต็อกของ**เมื่อวาน**ทั้งหมด
* **`sthour`** - สรุปรายการปรับปรุงสต็อกย้อนหลัง **1 ชั่วโมงล่าสุด**
  * *ผลลัพธ์:* ส่งแบบการ์ดประวัติอย่างละเอียด ระบุเวลาทำรายการ จำนวนหน่วยที่บวก/ลบ และข้อความหมายเหตุ (Note) ที่กรอกมา

### 5. คำสั่งเช็คงานพนักงาน (Staff Attendance & Leaves)
* **`staff`**
  * *การทำงาน:* เชื่อมต่อไปยัง HR API (`https://inthehaus-hr.vercel.app`) เพื่อดึงข้อมูลการลงเวลาของพนักงานประจำวัน
  * *ผลลัพธ์:*
    * แสดงสรุปการบันทึก **เช็คอินเข้างาน (🟢 เข้า) / เอาท์ออกงาน (🔴 ออก)** ของพนักงานทุกคน พร้อมแสดงไอคอนแสดงอารมณ์ตอนเช็คอิน (Mood Status)
    * แสดงรายชื่อพนักงานที่ทำการ **ลางานในวันนี้** พร้อมสถานะอนุมัติ (อนุมัติแล้ว/รออนุมัติ) และเหตุผลการลาอย่างชัดเจน
